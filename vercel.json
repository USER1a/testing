{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/movie-player run build",
  "outputDirectory": "artifacts/movie-player/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "functions": {
    "api/handler/index.ts": {
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/handler" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
